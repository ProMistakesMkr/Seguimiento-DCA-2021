class Figure {

    //atributos que heredan los circulos y cuadrados

    constructor(x, y, number, size) {
        this.x = x;
        this.y = y;
        this.number = number;
        this.size = size;
    }
}